---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { partners } from '@/lib/partners';
---

<BaseLayout
  title="Partenaires"
  description="Nos partenaires technologiques : SonicWall, Veeam, PRTG, WatchGuard, Dell, Vade et Arrow ECS. Des solutions de pointe pour votre système d'information."
  path="/partenaires"
>
  <section class="mx-auto max-w-4xl px-6 py-16 sm:py-20">
    <p class="eyebrow">Notre écosystème</p>
    <h1 class="mt-3 text-3xl font-bold tracking-tight text-content sm:text-4xl">Nos partenaires</h1>
    <p class="mt-6 leading-relaxed text-muted">
      Nos partenaires technologiques jouent un rôle essentiel dans notre capacité à répondre à vos
      besoins et à évoluer avec les défis du monde numérique. Leurs solutions de pointe enrichissent
      notre offre et nous permettent de vous proposer des services à la pointe de l'innovation.
    </p>
    <p class="mt-4 leading-relaxed text-muted">
      Chacun partage notre engagement envers la qualité, la fiabilité et la satisfaction client.
      Ensemble, nous vous fournissons des solutions intégrées et performantes — pour la sécurité, la
      gestion des données, les services cloud ou toute autre nécessité technologique.
    </p>
  </section>

  <section class="mx-auto max-w-6xl px-6 pb-20">
    <ul class="grid gap-6 sm:grid-cols-2">
      {
        partners.map((p) => (
          <li class="flex flex-col rounded-xl border border-edge bg-card p-6 shadow-sm">
            <div class="flex h-16 items-center rounded-lg bg-white px-3 dark:bg-white/90">
              <img
                src={p.logo}
                alt={`Logo ${p.name}`}
                loading="lazy"
                decoding="async"
                class="max-h-10 w-auto max-w-[170px] object-contain"
              />
            </div>
            <h2 class="mt-4 font-display text-lg font-semibold text-content">{p.name}</h2>
            <p class="mt-2 flex-1 text-sm leading-relaxed text-muted">{p.description}</p>
            <a
              class="mt-4 text-sm font-medium text-accent-600 hover:underline"
              href={p.url}
              target="_blank"
              rel="noopener noreferrer"
            >
              Visiter le site <span aria-hidden="true">↗</span>
            </a>
          </li>
        ))
      }
    </ul>
  </section>

  <section class="bg-panel">
    <div class="mx-auto max-w-4xl px-6 py-16 text-center">
      <h2 class="text-2xl font-bold tracking-tight text-content">
        Une technologie adaptée à vos besoins
      </h2>
      <p class="mx-auto mt-4 max-w-xl text-muted">
        Nous vous aidons à choisir et déployer les solutions les mieux adaptées à votre activité.
      </p>
      <a
        href="/contact"
        class="mt-8 inline-block rounded-full bg-content px-6 py-3 text-sm font-semibold text-surface transition hover:opacity-90"
      >
        Échanger avec nous
      </a>
    </div>
  </section>
</BaseLayout>
